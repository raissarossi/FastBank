import GitIconLight from '../../../Images/'

const IconsButtons = ({ title, name }) => {
    if (name == "github") { }
    if (name == "github") { }
    if (name == "github") { }
    if (name == "github") { }
    return(
        <button className='bg-pink-600 h-4 w-14'>

        </button>
    )
}

export default IconsButtons;